# Safe to expose in the browser. Never place a secret/service-role key here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
VITE_AI_ENABLED=false

# Configure AI_PROVIDER, GEMINI_API_KEY and GEMINI_MODEL only in Supabase Edge Function Secrets.
# Never add provider keys, service_role or other secrets to a Vite environment file.
